<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.immigration">

    <application
        android:name=".controller.application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.main.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.login.LoginActivity" />
        <activity android:name=".controller.login.ForgotPasswordActivity" />
        <activity android:name=".controller.login.EditProfileActivity" />
        <activity android:name=".controller.login.ResetPasswordActivity" />
        <activity android:name=".controller.login.ChangePasswordActivity" />
        <activity android:name=".controller.login.SignupActivity" />
        <activity android:name=".controller.login.OTPActivity" />
        <activity android:name=".controller.home.NavigationActivity" />
        <activity android:name=".controller.notification.NotificationActivity" />
        <activity android:name=".controller.statisticReport.StatisticReportActivity" />
        <activity android:name=".controller.generateReport.GenerateReport" />
        <activity android:name=".controller.subscription.SubscriptionActivity" />
        <activity android:name=".controller.subscription.SubscriptionListActivity"></activity>
    </application>

</manifest>